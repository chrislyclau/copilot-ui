node_modules/
build/
dist/
coverage/
.DS_Store
.env*
!.env.example
# CI-only dir created inside PR checkouts by .github/workflows/code-review.yml
# (the reviewer runtime runs from a base-branch checkout at this path).
.reviewer-runtime/
